#include "holberton.h"

/**
 * _islower - checks if c is lowercase
 *
 * @c: the number to be checked
 *
 * Return: 1 if c is lowercase
 */
int _islower(int c)
{
	if (c >= 97)
		return (1);

	return (0);
}
